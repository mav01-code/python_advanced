# Collection of modules - library
# Standard libraries - comes bundled with py, don't need to install it separately
# Third party library - Not included in py, need to install with pip

import django
print(django.get_version())
